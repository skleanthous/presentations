export interface BespokeSyncOption {
    key?: string;
}
export interface BespokeSyncState {
    reference: number;
    index: number;
    fragmentIndex: number;
}
export default function bespokeSync(opts?: BespokeSyncOption): (deck: any) => void;
