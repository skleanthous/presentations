export interface BespokeTouchOption {
    slope?: number;
    swipeThreshold?: number;
}
export interface TouchPoint {
    x: number;
    y: number;
    radian?: number;
    delta?: number;
}
export default function bespokeTouch(opts?: BespokeTouchOption): (deck: any) => void;
